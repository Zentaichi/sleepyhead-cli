# Non-secret configuration only. Copy to .env and adjust as needed.
# Never put OpenSSL passphrases, key material, or DB credentials in here.

LOG_LEVEL=info
BACKUP_DIR=./backups
LOG_DIR=./logs
STATE_DIR=./.sleepyhead-state
